
//eventのカレンダーの各日にちをクリックするとその日のイベントを表示するページに移動
/*
const event_days = document.querySelectorAll('#event-day');
for(let elem of event_days){
    elem.addEventListener('click',() => {
        const year = elem.getAttribute('data-year');
        const month = elem.getAttribute('data-month');
        const date = elem.getAttribute('data-date');

//        temp = toString(year)+ "年" + toString(month) + "月" + toString(date) + "日";
        temp = "年"+"月"+"日";

        window.location.href = "http://localhost:3000/event_day";

    },false);
};
console.log(temp);



const event_date = document.getElementById('event-date');
*/
/*
function goToNewPage(){
    year = this.year;
    month = this.month;
    date = this.date;
    window.location.href = "http://localhost:3000/event_day";
};
*/
